/**
 * Locale entry point for the MCP servers settings card.
 *
 * The dictionaries live in `./locales/en.ts`; this module is the import site
 * the card and its tests use, so the file layout stays free to grow a locale
 * per file without touching a caller.
 */

export { bind, en, zh, type LocaleKey, type Translate } from './locales/en.ts'
